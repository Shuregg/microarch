# Lab 2. Integrated Logic Analyser and Virtual IO

## Task 1

Создайте проект с free-running счётчиком. Используйте ILA для того, чтобы пронаблюдать за ним.


![alt text](image.png)

## Task 2

Добавьте к проекту со счётчиком сигнал enable, который будет завёдён с кнопки на плате. Не используйте пока логику debounce. Установите триггер на нажатие кнопки, пронаблюдайте на временных диаграммах дребезг.
Добавьте debounce логику, снимите временные диаграммы сигнала после неё. Что изменилось?

![alt text](image-1.png)

## Task 3

Создайте проект, состоящий из двух счётчиков (код задания 3 дан ниже). С помощью механизма capture захватите только переключения второго счётчика (counter 2)


![alt text](image-2.png)

## Task 4

Создайте проект, состоящий из двух счётчиков, при этом максимальное значение счётчиков задаётся снаружи. Вам необходимо создать враппер, в него включить блок VIO (создать его можно в IP каталоге) и сделать так, чтобы `counter1_max_i` и `counter2_max_i` задавались с помощью блока VIO. Покажите на временных диаграммах ILA работоспособность.
